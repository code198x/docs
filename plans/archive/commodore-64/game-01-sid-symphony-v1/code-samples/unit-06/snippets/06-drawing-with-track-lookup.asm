track_row_lo:
            !byte <(SCREEN + ROW_TRACK0 * 40)
            !byte <(SCREEN + ROW_TRACK1 * 40)
            !byte <(SCREEN + ROW_TRACK2 * 40)
track_row_hi:
            !byte >(SCREEN + ROW_TRACK0 * 40)
            !byte >(SCREEN + ROW_TRACK1 * 40)
            !byte >(SCREEN + ROW_TRACK2 * 40)
